# START HERE — Sawla Tours V11

This is the complete merge-ready Next.js replacement for the Ethiopia Tours / Journey Styles section.

1. Read `MERGE_OR_REPLACE_GUIDE_V11.md`.
2. Review `CHANGELOG_V11.md`.
3. Confirm the successful checks in `BUILD_VALIDATION_V11.md`.
4. Use `IMAGE_HANDOFF_JOURNEYS_V11.md` to install final licensed photography.
5. Run `npm ci && npm run type-check && npm run lint && npm run build` in the deployment environment.

The project contains no `.env` secrets and excludes `node_modules` and `.next` from the downloadable ZIP.
